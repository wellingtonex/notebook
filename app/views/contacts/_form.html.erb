<%= form_for(@contact, html:{class:"basic-grey"}) do |f| %>
   <h1><%= titulo %></h1>
  <% if @contact.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@contact.errors.count, "error") %> prohibited this contact from being saved:</h2>

      <ul>
      <% @contact.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome %>
    <%= f.text_field :nome %>
  </div>
  <div class="field">
    <%= f.label :email %>
    <%= f.text_field :email %>
  </div>
  <div class="field">
    <%= f.label :kind_id %>
    <%= collection_select(:contact, :kind_id, @kind_of_select, :id, :descricption) %>
  </div>
  <div class="field">
    <%= f.label :rmk %>
    <%= f.text_area :rmk %>
  </div>
  <div class="actions">
    <%= link_to 'Back', contacts_path, class:"button"%>
    <%= f.submit "Enviar", class:"button"%>
  </div>
<% end %>
